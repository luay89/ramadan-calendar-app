# Ramadan Calendar PWA - Backend Configuration
# انسخ هذا الملف إلى .env وقم بتعديل القيم

# المنفذ
PORT=3030

# مفاتيح VAPID (يجب توليدها مرة واحدة)
# قم بتشغيل: npm run generate-vapid
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# معلومات VAPID
VAPID_SUBJECT=mailto:admin@ramadan-calendar.app

# مسار قاعدة البيانات
DB_PATH=./data/ramadan.db

# المصادر المسموحة (CORS)
# افصل بين المصادر بفاصلة
ALLOWED_ORIGINS=http://localhost:3030,http://localhost:8080

# وضع التطوير
NODE_ENV=development
